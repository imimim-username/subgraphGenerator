# Direct runtime dependencies for subgraph-wizard.
# This file is the human-maintained source of truth; use floor bounds (>=) here.
#
# INSTALL (hash-verified, reproducible):
#   pip install --require-hashes -r requirements.txt
#
# DO NOT install from this file directly — it does not pin transitive deps.
#
# REGENERATE lockfile after editing this file:
#   pip-compile --generate-hashes requirements.in
#   (pip-tools must be installed: pip install pip-tools)
fastapi>=0.110.0
jinja2>=3.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
requests>=2.28.0
uvicorn>=0.27.0
